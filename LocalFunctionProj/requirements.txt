# azure-functions>=1.18.0
# transformers
# # torch @ https://download.pytorch.org/whl/cpu/torch-2.7.1%2Bcpu-cp311-cp311-manylinux_2_28_x86_64.whl
# sentence-transformers==2.2.2
# numpy==1.24.3
# psycopg2-binary==2.9.7
# requests
# rank_bm25
# botbuilder-core>=4.14.0
# botframework-connector>=4.14.0
# msgraph-sdk>=1.0.0
# msal
# anthropic
# psycopg2-pool==1.1
# # Text extraction dependencies
# Pillow
# pytesseract
# pdf2image
# pandas
# python-docx
# pdfplumber

# Core Azure Functions dependencies (always loaded)
azure-functions>=1.18.0

# Database connectivity (always loaded for most functions)
psycopg2-binary==2.9.7

# HTTP requests (lightweight, used in multiple functions)
requests

# Core bot framework (only loaded when teams_bot function is called)
botbuilder-core>=4.14.0
botframework-connector>=4.14.0

# Authentication and Graph API (only loaded when graph functions are called)
msgraph-sdk>=1.0.0
msal

# ML and NLP dependencies (only loaded when embedding/retrieval functions are called)
sentence-transformers==2.2.2
numpy==1.24.3
rank_bm25

# LLM providers (only loaded when specific LLM functions are called)
anthropic

# Text extraction dependencies (only loaded when extract_text function is called)
# These are the heaviest dependencies and benefit most from lazy loading
Pillow
pytesseract
pdf2image
pandas
python-docx
pdfplumber

# Optional database connection pooling (if used)
psycopg2-binary
